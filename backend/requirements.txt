# Core FastAPI stack
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9              # for file uploads if needed later

# SQLModel + async PostgreSQL driver
sqlmodel>=0.0.22
asyncpg>=0.29.0
alembic>=1.13.0                       # migrations

# Authentication & security
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Settings & env management
pydantic>=2.5.0
pydantic-settings>=2.5.2

# Utilities
python-dotenv>=1.0.0
email-validator>=2.1.0                # if you add email in future

# Optional but very useful for development
httpx>=0.27.0                         # for testing / external API calls
pytest>=8.3.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Optional: if you want richer OpenAPI docs
fastapi-users[sqlmodel]>=13.0         # optional - very convenient auth routes
# or use fastapi-users-db-sqlmodel if you prefer that variant
